[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.pnpm.toml"

[deploy]
# region = "us-west1"
runtime = "V2"
numReplicas = 1
# healthcheckPath = "/health"
# healthcheckTimeout = 300
sleepApplication = true
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
